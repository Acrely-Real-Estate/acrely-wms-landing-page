--- src/pages/ProductTourPage.tsx
+++ src/pages/ProductTourPage.tsx
@@ -118,22 +118,17 @@
         </aside>
 
-        {/* Mobile Accordion Navigation */}
-        <div className="lg:hidden w-full sticky top-16 z-30 bg-white/95 backdrop-blur-sm border-b border-slate-200 py-3 -mx-4 px-4 shadow-sm overflow-x-auto whitespace-nowrap">
-          <div className="flex gap-2">
-            {tourSections.map((section) => (
-              <button
-                key={section.id}
-                onClick={() => scrollToSection(section.id)}
-                className={`flex items-center gap-2 px-4 py-2 rounded-full text-xs font-bold border transition-colors ${
-                  activeSection === section.id 
-                    ? 'bg-[#1E40AF] border-[#1E40AF] text-white' 
-                    : 'bg-white border-slate-200 text-slate-600'
-                }`}
-              >
-                {section.title.split(' ')[0]} {/* Show just the number for brevity on mobile if needed, or keep full */}
-                {section.title.substring(3)}
-              </button>
-            ))}
-          </div>
+        {/* Mobile Dropdown Navigation */}
+        <div className="lg:hidden w-full sticky top-[4rem] z-30 bg-white/95 backdrop-blur-md py-4">
+          <select
+            className="w-full px-4 py-3 bg-white border border-slate-200 rounded-lg shadow-sm text-sm font-bold text-[#0F172A] focus:outline-none focus:ring-2 focus:ring-[#1E40AF] appearance-none"
+            value={activeSection}
+            onChange={(e) => scrollToSection(e.target.value)}
+          >
+            {tourSections.map(section => (
+              <option key={section.id} value={section.id}>
+                {section.title}
+              </option>
+            ))}
+          </select>
         </div>
 
